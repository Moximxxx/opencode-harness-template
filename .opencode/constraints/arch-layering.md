# 架构分层约束

> 强制三层架构依赖单向，禁止反向或平层交叉。

## 架构事实

本项目三层架构（项目初始化后根据实际技术栈调整）：

```
Layer 3: 前端/UI      — 用户界面层（展示与交互）
Layer 2: 业务逻辑     — 业务处理与编排层
Layer 1: 运行时       — 基础设施与运行时层
```

## 核心规则

### R-01: 禁止反向依赖

```
✓ 正确：Layer 3 → Layer 2 → Layer 1
✗ 错误：Layer 1 → Layer 2（运行时不能调用业务逻辑）
✗ 错误：Layer 2 → Layer 3（业务逻辑不能直接调用 UI）
✗ 错误：Layer 3 → Layer 1（前端不能直接调用运行时）
```

### R-02: 跨层通信必须通过接口

| 跨层场景 | 正确方式 |
|---------|---------|
| Layer 3 调用 Layer 2 | IPC / API 接口 |
| Layer 2 调用 Layer 3 | 回调函数 / 事件推送 |
| Layer 3 调用 Layer 1 | 间接通过 Layer 2 |

### R-03: 禁止循环依赖

```
✓ 正确：A → B → C
✗ 错误：A → B → C → A
```

## 验证方法

项目初始化后根据实际项目结构添加：

```bash
# 示例：检查前端是否直接调用了运行时 API
# grep -rn "require('...')" src/renderer/ 2>/dev/null
```
